# Copyright The bngtester Authors
# Licensed under the GNU General Public License v3.0 or later.
# SPDX-License-Identifier: GPL-3.0-or-later

name: bngtester

topology:
  nodes:
    bng1:
      kind: linux
      image: ${OSVBNG_IMAGE:-veesixnetworks/osvbng:latest}
      cap-add:
        - SYS_ADMIN
        - NET_ADMIN
        - IPC_LOCK
        - SYS_NICE
      env:
        OSVBNG_WAIT_FOR_INTERFACES: "true"
        OSVBNG_NUM_INTERFACES: "3"
      binds:
        - config/bng1/osvbng.yaml:/etc/osvbng/osvbng.yaml:ro

    server:
      kind: linux
      image: frrouting/frr:v8.4.1
      cap-add:
        - SYS_ADMIN
        - NET_ADMIN
      binds:
        - config/server/daemons:/etc/frr/daemons:ro
        - config/server/frr.conf:/etc/frr/frr.conf:ro
        - config/server/entrypoint.sh:/entrypoint.sh:ro
      exec:
        - bash /entrypoint.sh

    subscriber:
      kind: linux
      image: ${BNGTESTER_IMAGE:-veesixnetworks/bngtester:alpine-latest}
      cap-add:
        - NET_ADMIN
      env:
        ACCESS_METHOD: dhcpv4
        ENCAP: qinq
        PHYSICAL_IFACE: eth1
        SVLAN: "100"
        CVLAN: "10"
        MGMT_IFACE: eth0

  links:
    - endpoints: [bng1:eth1, subscriber:eth1]
    - endpoints: [bng1:eth2, server:eth1]
